name: ci
on:
  push:
    branches-ignore:
      - main
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run shellcheck
        run: shellcheck number_memory.sh